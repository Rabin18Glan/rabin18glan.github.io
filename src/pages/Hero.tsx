import { HeroContainer, ProfileImage, ProfileInfo } from "../features/hero";

function Hero() {
  return (
    <HeroContainer>
      <ProfileImage />
      <ProfileInfo />
    </HeroContainer>
  );
}

export default Hero